# The existence of this file marks this directory as a Python package so `scripts`/`scripts.shared`
# can be imported (required for Hatchling's `packages = ["scripts"]` build to produce real subpackages).
